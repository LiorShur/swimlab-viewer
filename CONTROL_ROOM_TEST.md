# Control Room — PR detection test

This throwaway file exists only to open a pull request so the Control Room
dashboard can be seen picking up a newly opened PR:

- the **Open PRs** tile increments, and
- this repo's card shows a **PR #N open** flag.

Safe to close this PR and delete the `claude/project-admin-dashboard-nuezk5`
branch once verified.
